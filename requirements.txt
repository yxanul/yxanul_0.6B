# Core dependencies for Yxanul training
# Note: Use NVIDIA NGC container (nvcr.io/nvidia/pytorch:24.08-py3) for best results
# That container includes PyTorch, CUDA, Transformer Engine, and Flash Attention

# Essential packages
transformers>=4.40.0
datasets>=2.15.0
accelerate>=0.25.0
wandb
pyyaml
tqdm

# Optional optimizations (included in NGC container)
# transformer-engine  # For FP8 support
# flash-attn         # For Flash Attention
# apex               # For mixed precision